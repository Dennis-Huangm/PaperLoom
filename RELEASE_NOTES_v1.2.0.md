# arXiv Research Assistant v1.2.0

发布日期：2026-08-22

本版本重点统一“完整阅读报告”和 Obsidian 文献库两类长期知识资产。

## 应用图标与并行任务

- 新增暖橙色开放书本与知识节点图标，提供透明 PNG 和包含 16、24、32、48、64、128、256 像素图层的 Windows ICO。
- GUI 品牌标志、favicon 与安装包静态资源统一使用新图标。
- 后台任务支持 1–8 路动态并行，默认 3；可在配置中心实时修改。
- 同一目标的重复任务自动合并；Obsidian 写入与 Semantic Scholar 访问具备并发保护。

## 完整阅读报告

- 固定一级结构：基本信息表、一句话总结、为什么值得阅读、研究问题与背景、核心方法、主要贡献、实验设置、关键结果、与已有工作的区别、局限性、可复现性。
- 图示讲解只由定稿器生成，并且只出现在“核心方法 → 方法图解析”。
- 方法图下方不再展示 `arXiv HTML` 或 `PDF 第 N 页` 等内部来源标签。
- “核心方法”正文进一步解释方法总览、模块数据流与训练/推理差异；关键公式逐一说明符号、计算顺序、设计动机以及对训练信号或推理决策的作用。
- 自动清除模型生成的第二套 `Figure N` 小节，历史报告可通过重建脚本规范化。
- ToolArtist（2608.04436）报告已重建，Figure 1–3 不再被重复解释。

## Obsidian 文献库

- 推荐目录：`Home`、`Daily`、`Papers`、`Topics`、`Reviews`、`Attachments`、`System`，不再添加数字排序前缀。
- 论文笔记按研究方向归档，并使用 `ToolArtist.md`、`VCode.md`、`GenArtist.md` 这样的知识名称。
- 对没有明确方法名的论文，使用可读的论文标题；arXiv ID 不再出现在文件名中。
- 每日推荐按 `YYYY/MM` 归档，周报按年份归档，`Research Hub.md` 提供统一入口。
- 继续保留托管区外的个人笔记，并在同名非托管文件冲突时使用安全备用名。

## 本机应用

应用可将用户指定的本地 Obsidian vault 根目录作为知识库根；同步不会迁移、覆盖或删除其他 vault。

前端同时修复了报告库搜索图标与输入框的对齐，并让顶部导航在窄窗口下自动收起为菜单按钮，避免品牌、导航、搜索和筛选控件重叠。

新增 4K/超宽屏响应式布局：在 CSS 视口宽度达到 2000px 时，主画布扩展至 2400px，工作区使用可视高度而不再受 920px 上限约束，并适度放大导航、论文列表、详情文字和操作按钮。普通 1080p 与移动端布局保持原有尺寸。

每日推荐的 arXiv API 请求现在会对连接重置、传输超时、HTTP 429 和临时 5xx 错误执行最多 3 次指数退避重试；连续失败时显示明确的中文服务错误，避免一次 WinError 10054 直接终止任务。

刷新每日推荐不再阻塞“文献推荐”页面：摘要模型调用在缓存锁之外执行，页面请求只读取缓存；新推荐完成摘要本地化后才原子替换旧文件，因此刷新期间仍可继续浏览上一份完整推荐与历史日期。

首页推荐详情现在将英文摘要提炼为 120–220 字、完整显示的“摘要精要”，并额外生成 100–180 字的研究方向感知“推荐依据”。后者必须指出论文与当前方向的具体连接、关键机制或证据，以及可迁移的研究启发。系统对当日缺失的内容执行一次批量模型请求，并按研究方向缓存在 `run/abstract-translations.json`；后续页面加载不会重复消耗 API。已有完整报告时，操作区会显示“重新生成报告”和“打开报告”。

Obsidian 同步会自动把网页报告使用的 KaTeX 分隔符转换为 Obsidian MathJax 语法，同时保护 fenced code 与行内代码。已有论文笔记执行一次全量同步即可修复公式显示。

后台生成完整报告后，“打开结果”会优先打开渲染后的 `report.html` 阅读页面，而不是 Markdown 源文件。

首页推荐现在严格按当前研究方向读取历史结果。新增“文献库”页面和“加入文献库”操作；收藏数据按方向保存，不受每日推荐刷新影响，并可搜索、打开报告/arXiv/PDF、存入 Zotero 或移出。

文献推荐页新增“每日记录”月历选择器：有推荐结果的日期高亮并标注论文篇数，无结果日期置灰不可点击；支持切换月份和一键返回最新日期。选择日期会同时更新列表、论文详情、右上角数量与批量 Zotero 保存范围，默认仍打开最新一天。

阅读偏好现已收敛为两个互斥动作：加入文献库即视为相关正样本，“不相关”即为负样本。后续推荐只参考当前方向最近收藏的 30 篇论文及不相关负样本，并在候选阶段排除紧密匹配负样本的论文；报告列表与完整报告阅读页均可直接加入文献库，文献库会按状态显示“打开论文报告”或“生成论文报告”。

从报告加入文献库时，系统现在会优先复用当前方向的摘要缓存；若没有缓存，则从完整报告的“一句话总结”和“为什么值得阅读”提取中文回退内容，并在模型可用时生成与推荐页一致的摘要精要与保存价值。打开文献库也会自动补齐早期收藏的缺失字段。

发布冻结审查进一步拆分了 Web 数据读取、配置校验和后台任务队列，并把每日流水线整理为排名、选择、核验、发布和集成五个阶段。同一天的推荐现在按研究方向独立保存；所有关键 JSON/配置/Obsidian 托管笔记使用原子替换，SMTP 发送失败时不会提前提交去重状态。源码包改由 `scripts/build_release.ps1` 生成并自动拦截 `.env`、`config.yaml`、`profiles/`、`run/`、缓存与字节码。

当 arXiv API 长时间返回 429 或连接失败时，可在配置中心启用 alphaXiv 备用检索并在 `.env` 配置 `ALPHAXIV_API_KEY`。alphaXiv 只负责发现候选论文，正式出版信息仍由 OpenAlex、Semantic Scholar、arXiv 页面等来源核验。

alphaXiv 的真实 `discover_papers` 响应可能使用编号 Markdown，而不是 JSON；本版本已同时兼容两种格式。

备用请求会附带当前研究方向的历史 arXiv ID 排除清单，并对返回结果再次本地去重；如果仍没有新论文，系统不会再覆盖当天文件并生成空日报。

## 验证

- 自动化测试：98 passed。
- Obsidian：16 篇论文笔记，39 个受管笔记，0 个断链，0 个 arXiv ID 文件名。
- ToolArtist HTML：1 个方法图解析区、3 个 Figure 子项、无重复 Figure H3、无横向溢出。
