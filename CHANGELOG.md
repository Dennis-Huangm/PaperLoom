# Changelog

所有重要变更记录在此文件中。

## 1.3.0 - 2026-09-16

### Added

- 新增 `hybrid` 协作检索：arXiv 与 alphaXiv 共同提供候选，合并去重后使用 arXiv 补全元数据，并保留发现来源和补全状态。
- 统一论文数据解析与本地完整元数据缓存；历史推荐使用所选快照，直接输入无版本 ID 获取最新版本，显式版本号严格匹配。
- 按研究方向统一收藏与负反馈状态，使用跨进程锁和原子写入，并兼容读取旧状态文件。
- 后台任务在提交时捕获研究方向和配置；研究服务客户端统一管理延迟创建与资源释放。

### Changed

- 项目对外名称更新为 **PaperLoom · 知织**，标语为“把论文织成知识”。README、本地界面、新生成的报告、测试邮件与集成展示名称同步更新。
- 新增 `paperloom` CLI 入口，保留 `arxiv-ra`、`arxiv_ra` 模块名及 `arxiv-research-assistant` 发行包名，兼容已有安装与脚本。
- 新配置模板使用 PaperLoom 作为 Zotero 分类和 Obsidian 根目录名称；已有配置、旧默认目录、Obsidian 托管标记及 Windows 计划任务标识不自动迁移。
- 新源码归档使用 `paperloom-<version>-source.zip`，历史版本说明与已有生成文件保留原有名称。
- GitHub 仓库更名为 `Dennis-Huangm/PaperLoom`，README 增加新仓库的克隆与 Release 下载入口。
- GitHub Actions 日报改为仅手动触发，通过 `PAPERLOOM_CONFIG_YAML` Secret 准备配置；报告 artifact 默认不上传。

### Fixed

- 修复并发收藏、负反馈和页面修复之间的状态竞争，避免已删除论文被重新加入文献库。
- 报告、周报、引用关系与版本追踪按研究方向隔离；论文元数据、PDF 和方法图使用一致版本。
- 完善 Actions 中的 alphaXiv 密钥映射与方向数据缓存，移除要求提交个人配置的错误提示，停止未配置仓库的周期性失败通知。
- 源码发布包排除生成的 `*.egg-info` 与个人专用配置示例，避免携带旧构建元数据或本地配置。
- 修复 PowerShell 打包时字节码筛选误删暂存源码的问题；使用 .NET 检查 ZIP，并验证关键文件存在，防止发布空源码包。

### Validation

- 发布前完整离线测试通过：153 项；保留 1 项已有 Starlette/httpx 弃用提示。

## 1.2.0 - 2026-08-22

### Changed

- 文献推荐页新增按研究方向隔离的“每日记录”月历选择器；有结果的日期高亮并标注篇数，无结果日期置灰，历史日期会同步切换论文列表、详情、数量与 Zotero 批量保存范围。
- 新增暖橙色“开放书本 + 知识节点”应用图标，提供透明 PNG、多尺寸 ICO，并接入 GUI 品牌标志与 favicon。
- 后台队列由固定单线程升级为 1–8 路动态并发调度，默认并行 3 个任务；配置保存后立即生效。
- 相同类型与目标的重复任务自动合并；Obsidian 写入串行保护，Semantic Scholar 在所有并发任务间共享限流。
- 统一完整阅读报告的一级章节顺序，并由定稿器独占“核心方法 → 方法图解析”的生成职责。
- 清除报告模型重复创建的 `Figure N` 小节；重复定稿仍只保留一套方法图与通俗解读。
- 删除每张方法图下方的 `Figure N · arXiv HTML/PDF 第 N 页` 内部来源标签。
- 加强“核心方法”生成约束：独立解释方法数据流、模块依赖和训练/推理差异，并逐式说明关键公式的符号、计算过程、设计动机与优化作用。
- Obsidian 知识库升级为首页、时间流、论文、主题、综述、附件、系统索引的语义分层结构，并移除目录数字前缀。
- 论文笔记按研究方向归档，文件名优先使用方法/模型名，否则使用可读论文标题；arXiv ID 仅保留在 frontmatter 与正文。
- 每日推荐按年/月归档，周报按年归档，首页升级为 `Research Hub.md`。
- 首页按当前研究方向读取各自最近一次推荐，切换方向不再显示其他方向的全局最新结果。
- 新增按研究方向隔离的“我的文献库”和显式收藏操作，保存论文快照、中文摘要、推荐依据、反馈与报告入口。
- 顶部“文献”入口更名为“文献推荐”，与长期保存的“文献库”明确区分。
- 阅读偏好精简为两个互斥信号：加入文献库即视为相关正样本，“不相关”作为负样本；移除不再使用的必读、稍后读和已读状态，并在候选阶段排除紧密匹配负样本的论文。
- 推荐排序使用当前方向最近收藏的 30 篇论文作为正样本，避免大型文献库造成偏好漂移。
- 报告列表与完整报告阅读页新增“加入文献库”；文献库按报告状态显示“打开论文报告”或“生成论文报告”。
- 统一推荐页与报告页的收藏数据：报告来源论文会复用摘要缓存或完整报告内容补齐中文摘要精要与保存价值，文献库页面自动修复既有缺失字段。

### Fixed

- 发布冻结审查将 Web 数据目录、配置解析和后台队列从单体 `web.py` 拆为独立模块，并将每日流水线拆成可单测阶段，保持原有公开导入兼容。
- JSON、配置、方向档案及 Obsidian 托管笔记统一使用短临时名的原子替换，避免中断时暴露半写文件，并兼容 Windows 深层长路径。
- 同一天运行多个研究方向时，推荐结果改为按方向独立保存；月历历史与 Obsidian 全量同步不会再被当天最后运行的方向覆盖。
- 请求发送邮件但 SMTP 失败时不再提前提交去重状态，下次运行可以正常重试同一批推荐。
- 文献库页面修复旧条目时仅使用缓存和本地报告内容，不再在 GET 页面等待模型网络请求。
- 论文 PDF 改为下载到短随机临时文件，校验响应大小后再原子替换；网络中断或错误页面不会破坏已有完整 PDF。
- 目录读取遇到单个损坏的历史 JSON 时会跳过该条目并继续展示其他结果，不再令整个页面返回 500。
- arXiv API 请求增加进程级 3 秒节流和 429 冷却退避；并行任务不会共同撞击 API 限流，最终错误会明确提示当前是限流而非普通连接失败。
- 新增可选 alphaXiv 备用发现源：仅在 arXiv API 重试耗尽后调用 MCP `discover_papers`，候选仍经过本项目的方向排序、去重和元数据核验。
- 兼容 alphaXiv `discover_papers` 实际返回的编号 Markdown 格式（`[ID=...] **Title** ...`），不再因预期 JSON 而错误提示“未返回可用论文”。
- alphaXiv 备用检索会把当前方向的历史推荐 ID 加入排除条件，并在本地再次去重；若最终没有新论文，将阻止发布误导性的空日报并返回明确原因。
- 完整阅读报告任务在 arXiv API 失败时会优先复用当前方向的推荐快照、文献库或已有报告元数据，不再因为重复查询 arXiv ID 而失败。
- 对本地未知的单篇报告 ID，增加 arXiv → alphaXiv 精确 ID 解析；创建研究方向时的参考论文也使用相同回退，全部来源失败时仍保留用户输入的种子 ID。
- 历史日期上的收藏、反馈和 Zotero 操作会遍历当前方向的推荐历史并保留真实推荐日期，不再只查最新一天。
- 周报改为读取当前方向的独立推荐文件，同一天切换多个研究方向时不会混入最后运行方向的论文。
- 新安装不再硬编码任何个人研究方向；默认方向名称和 ID 会由用户填写的研究主题自动生成。
- 修复刷新每日推荐期间“文献推荐”页面被摘要缓存锁阻塞的问题：模型网络调用移出缓存锁，页面只执行快速缓存读取；新推荐在本地化完成后才原子发布，任务进行中继续展示上一份完整结果。
- arXiv API 检索遇到连接重置、传输超时、HTTP 429 或临时 5xx 时自动执行最多 3 次指数退避重试，连续失败后返回可理解的中文错误，不再因一次 WinError 10054 直接终止每日推荐。
- 新增宽度 2000px 以上的大屏布局：主画布扩展至 2400px、解除工作区 920px 高度上限，并同步放大导航、列表、详情文字与操作按钮；1080p/窄屏断点保持不变。
- 修复 ToolArtist 报告中 Figure 1–3 被解释两遍的问题。
- 修复重新插入方法图块时可能将后续一级章节粘连到前一段落的问题。
- 修复报告库搜索框的双层边框和搜索图标偏移。
- 修复窄窗口下品牌、主导航和页面控件互相重叠；导航在 1100px 以下改为可展开的横向菜单。
- Obsidian 导出时将网页报告的 `\\(...\\)` / `\\[...\\]` 数学分隔符转换为 `$...$` / `$$...$$`，并跳过代码块与行内代码。
- 后台报告任务的“打开结果”优先跳转同目录的 `report.html`，不再直接打开 `report.md`。

### Dashboard

- 推荐详情改为 120–220 字的“摘要精要”，不再逐段翻译原摘要，也不再使用行数截断。
- 新增 100–180 字的研究方向感知“推荐依据”，具体说明连接点、关键机制/证据和可迁移启发；缓存按研究方向隔离。
- 已有完整报告的论文显示“重新生成报告”和“打开报告”；未生成报告的论文仍显示“生成完整报告”。

### Validation

- 104 项自动化测试通过。
- ToolArtist 实际 HTML 中方法图解析 1 处、Figure 子项 3 个、横向溢出 0。
- 实际 Obsidian vault 验证中，16 篇论文笔记无 arXiv ID 文件名，39 个受管笔记无断链。

## 1.1.0 - 2026-08-22

### Added

- Obsidian vault 自动发现与配置中心路径选择。
- 每日推荐、论文元数据、完整阅读报告、研究周报和阅读反馈自动同步。
- YAML frontmatter、`[[双向链接]]`、研究方向页、概念页和论文/每日/周报 MOC 索引。
- Obsidian 论文笔记使用模型精炼中文摘要，不再写入冗长的 arXiv 原文摘要；摘要结果按内容签名缓存。
- 方法图复制与 Obsidian 附件 wikilink 重写；PDF 可选复制。
- 后台全量同步任务、CLI `obsidian-sync`、系统状态与 `obsidian://` 打开入口。

### Safety

- 只写入 vault 下的独立专用目录，不读取或修改 `.obsidian` 与其他用户笔记。
- 用户编辑区位于托管标记之外，重复同步不会覆盖。
- 与已有非托管同名笔记冲突时自动使用安全的备用文件名。
- 禁止绝对子目录和 `..` 路径逃逸；写入前验证所有目标仍在专用根目录内。
- 默认不执行 git commit/push，默认不复制 PDF。

## 1.0.0 - 2026-08-19

### Added

- 多研究方向档案：根据关键词和参考 arXiv 论文生成、保存与切换检索配置。
- 每日 arXiv 检索、关键词预筛、LLM 精排、按方向隔离的去重状态和 QQ 邮件推荐。
- OpenAlex、Semantic Scholar 与 arXiv 声明的出版元数据核验。
- 主动生成结构化中文阅读报告，支持本地 KaTeX、侧边目录和 arXiv HTML → PDF 方法图回退。
- Zotero 10+ 本地 API：授权、条目去重、每次选择分类、PDF 导入、报告附件和批量保存。
- 阅读反馈闭环：必读、相关、稍后读、已读和不相关反馈参与后续排序。
- 每周研究综述：趋势、方法簇、结论分歧、开放问题和必读清单。
- arXiv 版本追踪与新旧 PDF 差异报告。
- Semantic Scholar 相关工作图谱，以及本地相似度空间、全节点标签避碰和可选引用边。
- 本地 FastAPI/Jinja GUI、后台任务、状态检查、Windows 计划任务和 GitHub Actions。

### Security

- GUI 仅监听 `127.0.0.1`，拒绝跨站写请求并设置安全响应头。
- API key、邮箱与授权码仅保存在 `.env`，网页不回显现有值。
- Zotero 本地 API 和 PDF 上传地址限制为回环地址。
- 正式源码包排除 `.env`、`config.yaml`、`run/`、构建缓存和用户报告。

### Fixed

- Semantic Scholar 每秒请求限制和 429 退避重试。
- arXiv 版本、推荐与 Zotero 条目的重复处理。
- 报告公式、方法图选择、主图位置和报告阅读布局。
- 交互图谱节点/标签碰撞、边界堆积、缓存回退和图层开关。
- GitHub Actions 跨运行恢复推荐、反馈、版本与周报状态，并补充 QQ 邮箱变量。
